# LatencyOS 設計書 v2.0
### ― 決定論的低遅延パイプラインを核に備えたハードリアルタイム汎用実験OS ―

---

## 0. エグゼクティブサマリ

LatencyOS は、**決定論的低遅延ストリーミングパイプラインを核に持ちつつ、時間駆動型シェル・独自リアルタイム言語・内蔵エディタ・静的ファイルシステムを備えたハードリアルタイム汎用実験OS** である。

Linux や Windows などの汎用 OS との根本的な違いは：

> **「平均スループットを最大化する」のではなく、「最悪ケースの遅延（WCET）を保証し、ジッタを完全に排除する」ことをコア原則とする。**

ストリーミングパイプライン（Core 1〜3）では動的メモリ確保やプリエンプティブなスケジューラを一切排除し、8.0msのGlass-to-Glassレイテンシ予算を死守する一方で、Control Plane（Core 0）では対話型シェル（Pulse Shell）、独自64-bitレジスタマシンアーキテクチャ（`px64`）、カーネル内蔵エディタ（PulseEditor）、および静的ファイルシステム（LatencyFS）を提供し、低遅延処理のプログラミング・実験・検証をカーネル内で完結できるように設計されている。

対象ユースケース例:
- クラウドゲーミング / リモートデスクトップの超低遅延送受信アプライアンス
- 競技eスポーツ用の入力→キャプチャ→表示パイプライン実験機
- 産業用ビジュアルサーボ（カメラ→画像処理→アクチュエータ）
- 金融の低遅延ティッカー解析・発注系
- ハードリアルタイム言語・アーキテクチャの研究開発環境

---

## 1. 差別化ポイント（既存OSとの比較）

| 観点 | Linux (PREEMPT_RT含む) | Windows | **LatencyOS** |
|---|---|---|---|
| スケジューラ思想 | 公平性(CFS)＋RT拡張 | 汎用・優先度ベース | **静的コア割当。スケジューラそのものが存在しないコアがある** |
| 割込み処理 | ソフトIRQ経由が基本 | DPC遅延あり | **ISRから直接ユーザー空間へcompletion（epoll不要）** |
| デバイスドライバ | カーネル内 or カーネルモジュール | カーネルドライバ + ミニポート | **ユーザー空間ポーリングドライバ（UIO/VFIO類似、割込み最小化）** |
| ネットワークスタック | 汎用TCP/IPスタック | 汎用TCP/IPスタック | **カーネルバイパス＋QUIC/WebRTCネイティブ実装。TCPは非対応** |
| メモリ管理 | ページフォルト許容、スワップあり | 同左 | **起動時に全メモリを事前マッピング。実行時ページフォルト0を保証** |
| GPU連携 | ドライバスタック経由（複数レイヤ） | 同左（WDDM経由） | **GPU capture/encodeエンジンをOSのファーストクラス概念として直接公開** |
| 電源管理 | C-state遷移あり | 同左 | **C-state遷移を禁止（クロックゲーティングのジッタを排除）** |
| 対象範囲 | 汎用 | 汎用 | **単一アプリケーション専用アプライアンスOS（マルチユーザー非対応）** |

要するに、Linuxが「RTパッチを当てて汎用OSをRTに寄せる」アプローチなのに対し、LatencyOSは **最初から汎用性・公平性・省電力を持たないことを設計原則にする** ことで差別化する。

---

## 2. コアコンセプト：「レイテンシ予算駆動設計」

すべての設計判断は以下の予算表に対して行う（1080p60fps、目標 glass-to-glass 8ms以下を想定）。

```
入力イベント発生         → 0.0ms
  ISR起床                 → 0.05ms  (静的優先度、割込み禁止区間なし)
  ユーザー空間へ通知       → 0.15ms  (futex的機構ではなく専用IPC)
GPU処理開始              → 0.3ms
画面キャプチャ完了        → 2.0ms  (DXGI Desktop Duplication同等API)
NVENCエンコード完了       → 4.5ms  (ゼロコピーDMA、GPUメモリ直結)
ネットワーク送出          → 5.0ms  (カーネルバイパスNIC、割込み契機なし)
--------------------------------------------------------
合計予算                 8.0ms 以内
```

各サブシステムはこの表の「持ち時間」を守るためだけに設計される。守れない機能は原則として実装しない。

---

## 3. アーキテクチャ全体像

### 3.1 カーネル形態：**ハイブリッド・ユニカーネル**

マイクロカーネルの「分離」もモノリシックカーネルの「性能」も帯に短し襷に長し、なので次の折衷案を採用：

- **Core 0**: 管理用マイクロカーネル（プロセス生成、メモリ管理の統括、フォールトハンドリング）
- **Core 1〜N-1**: ユニカーネル的に「1コア＝1アプリケーション専用」。OSサービスはライブラリとしてアプリケーションに静的リンクされ、カーネル/ユーザー境界を横断するコンテキストスイッチが物理的に発生しない

```
┌─────────────────────────────────────────────┐
│ Core 0: Control Plane (マイクロカーネル)       │
│  - ブート・初期化                             │
│  - コア/デバイスの静的割当決定                 │
│  - 障害監視（ウォッチドッグ）                  │
│  - Pulse Shell / PulseEditor / LatencyFS    │
│  - px64 レジスタVMコンパイラ・実行エンジン      │
├─────────────────────────────────────────────┤
│ Core 1: Capture Domain (ユニカーネル)          │
│  - GPU capture library をリンク                │
│  - 割込みはこのコアに固定（IRQ affinity）       │
├─────────────────────────────────────────────┤
│ Core 2: Encode Domain (ユニカーネル)           │
│  - NVENCドライバライブラリを直接リンク          │
├─────────────────────────────────────────────┤
│ Core 3: Network Domain (ユニカーネル)          │
│  - カーネルバイパスNICドライバ + QUIC実装       │
└─────────────────────────────────────────────┘
```

各ドメイン間通信は共有メモリ上の **SPSC(Single Producer Single Consumer) lock-free リングバッファ** のみ。ミューテックス・セマフォは原則禁止（優先度逆転のリスクを構造的に排除）。

### 3.2 なぜこの形態が差別化になるか

- Linuxのコンテナ/cgroupは「リソースを分ける」が「コンテキストスイッチのコスト」は残る
- LatencyOSは「そもそもスケジューラを介さない」ため、コンテキストスイッチという概念自体がCore 1以降に存在しない
- 障害分離はハードウェアのコア境界そのものに委ねる（MMUによるアドレス空間分離は維持）

---

## 4. スケジューリング設計

汎用OSのスケジューラ（CFS, MLFQ等）を **廃止** する。代わりに：

- 起動時にコア数分の「専用ロール」を静的に決定（capture / encode / network / control）
- 各ロールは自コア上で **ポーリングループ**（busy-wait）を回す。割込み待ちでスリープしない
- これにより「スケジューラが起こしてくれるまでの待ち時間」というレイテンシ要因そのものを消す
- トレードオフ：CPU使用率は常に100%（電力効率は最初から放棄。差別化の核心）

### 4.1 Core 0（Control Plane / Pulse Shell）の対話・実行上限とウォッチドッグへの影響

Core 0 は UART 16550 のラインステータスレジスタ（LSR）をスピンポーリング（`_mm_pause()` 駆動、割り込みスリープなし）で常時監視し、キー入力を 1 バイト単位で即座に処理する。スリープやコンテキストスイッチを行わないため、シェルの通常コマンドディスパッチは **最悪 500 us 以内**（通常 18 ns 〜 45 us、64エントリ走査 `ls -t` 実測 45 us に対する安全マージン）で完了する。

Core 0 上で実行される各種対話処理・スクリプト実行には、以下の厳格な実行上限（タイムアウトおよびバッファ制限）が設けられている：

1. **スクリプト実行時間上限 (`run <file>`)**:
   - `px64` 仮想マシン（`PX64VM`）には **「動的実行ステップ数上限（`MAX_VM_STEPS = 10,000`）」** と **「TSC 壁時計タイマー（`MAX_SCRIPT_TIMEOUT_NS = 5,000,000`、5.0ms デッドライン）」** の二重安全装置が実装されている。
   - **命令種別ごとの WCET 内訳**:
     - 純粋演算・比較・分岐命令: 1命令あたり 2〜15 ns（最悪 25 ns）。10,000 ステップで $\le 0.25\text{ ms}$。
     - 軽量レジスタ/UART Intrinsics（`@tsc`, `@rtt`, `@rate`, `@argc`, `@arg`, `@print`, `@println`）: 5〜500 ns。
     - `@capture()`: ノンブロッキング最新フレーム取得（5スピンタイムアウト）＋ 64KB CRC32 整合性計算 $\approx \mathbf{480\,\mu\text{s}}$（約 0.48 ms）。Core 0 の長時間停止を防ぐため、60Hz VBLANK（最大 16.6ms）のブロッキング待機は行わず、タイムアウト時は最新フレームスロットを即時返却する。
     - `@send(#f)`: 完全な SRTP 送出（45 MTU パケット分割、各パケットの AES-128-GCM ハードウェア暗号化 ＋ GMAC 認証タグ ＋ e1000 NIC DMA 送出） $\approx \mathbf{31.0\,\mu\text{s}}$（約 0.031 ms）。
   - **スクリプト実行のトリガー関係と最悪保証値**:
     - 通常スクリプト（`stream.pl` $\approx 0.55\text{ ms}$, `echo.pl` $\approx 0.05\text{ ms}$）: どちらの上限にも達せず正常完了する。
     - 純粋演算無限ループ: 10,000 ステップ上限（$\le 0.25\text{ ms}$）に先に到達し、`ERR_PX64_WCET_EXCEEDED` で即座に強制中断される。
     - ハードウェア Intrinsics 無限ループ（`@capture()` ループ等）: 約 10〜11 反復（$\approx 5.0\text{ ms}$）時点で TSC 壁時計タイマーが先に到達し、`ERR_PX64_TIMEOUT_EXCEEDED` で強制中断される。
     - **スクリプト実行の真の物理最悪値**: タイムアウト検査はディスパッチループ先頭で行われるため、最悪値は「5.0ms（タイマー閾値）＋ その直前に開始した最長単一命令の所要時間（`@capture()` 0.48ms）」＝ **約 5.48 ms** となる（実測検証済み）。
2. **エディタ一括ペーストの処理時間と上限 (PulseEditor)**:
   - エディタのテキストバッファ上限は **最大 4,096 バイト (`MAX_EDITOR_BUF = 4096`)** に制限されている。
   - ペースト時の文字挿入および画面 ANSI 再描画処理はペースト長に比例（$O(N)$）するが、バッファ上限である 4KB 全域を一括ペーストした場合でも **最悪 5.0 ms 以内** に完了する。
3. **ウォッチドッグ監視間隔への影響**:
   - Core 0 がスクリプト実行（最悪 5.48 ms）またはエディタの全バッファペースト（最悪 5.0 ms）を処理している間、同一コア上で動作するウォッチドッグおよびテレメトリ集計の監視間隔は **最悪ケースで最大 5.48 ms 遅延（停止）しうる** が、パイプライン駆動中の Core 1〜3 は独立したハードウェアポーリングループで自律動作するため、ストリーミング側の Glass-to-Glass 8.0 ms 予算には影響を与えない。

```c
// Core 1 (Capture Domain) のメインループ概念コード
void capture_domain_main(void) {
    pin_to_core(1);
    disable_interrupts_except(GPU_VBLANK_IRQ);
    set_cstate_lock(C0); // C-state遷移を禁止

    while (true) {
        if (gpu_frame_ready_flag) {   // ポーリング。割込み待ちしない
            capture_frame_zero_copy();
            ring_buffer_push(&capture_to_encode_ring, frame_handle);
            gpu_frame_ready_flag = false;
        }
        _mm_pause(); // busy-wait hint
    }
}
```

---

## 5. メモリ管理

- **起動時に全物理メモリをマッピング済み** にする。ページフォルトは実行時に一切発生させない
- ヒープアロケータは動的malloc/freeを使わず、**固定サイズプール＋事前確保** のみ
- ガベージコレクションは当然非搭載（言語もGC非搭載のものを選定：後述）
- Huge Pages (1GB TLB) をデフォルトで強制し、TLBミスを構造的に排除

---

## 6. デバイスドライバ戦略

### 6.1 ユーザー空間ポーリングドライバ

- Linuxの VFIO / UIO の思想を踏襲しつつ、**カーネルへの割込み転送すら省く**
- GPU: DXGI Desktop Duplication / NVENC相当のレジスタ叩き込みをユーザー空間ライブラリとして直接実装（既存プロジェクトの知見がそのまま転用できる領域）
- NIC: DPDK的なポーリングモデルをOS標準機能として最初から組み込む（後付けライブラリではなくOSのファーストクラス機能にする点が差別化）

### 6.2 対応デバイスは意図的に絞る

汎用OSが目指す「あらゆるハードウェアへの対応」を放棄。対応するのは：
- NVIDIA GPU（NVENC搭載世代のみ）
- 特定世代のNIC（Intel/Mellanox、カーネルバイパス対応品のみ）
- それ以外は「非対応」と明言する。これも差別化（Windowsは絶対にできない割り切り）

---

## 7. ネットワークスタック

- TCP/IPスタックの汎用実装は搭載しない
- **QUIC（信頼性のある輻輳制御付き）** と **WebRTC (SRTP/DTLS)** をOSカーネルの一部としてネイティブ実装
- ソケットAPI（BSD socket互換）は提供しない。代わりに「フレームを渡すとネットワークに乗る」という単一目的APIのみ提供

```c
// LatencyOS ネットワークAPI（単一目的に絞った例）
netresult_t stream_send_frame(encoded_frame_t* frame, deadline_ns_t deadline);
```

---

## 8. 起動プロセス

汎用OSのブート（BIOS/UEFI → ブートローダ → カーネル初期化 → initプロセス → サービス起動...）は数百msから数秒かかる。LatencyOSは「常時起動アプライアンス」を前提とし、起動時間の最適化は優先度を下げる代わりに、**起動後のレイテンシ特性を起動直後から即座に発揮する** ことを重視：

1. UEFIから直接カーネルイメージをロード（ブートローダ層を極力薄く）
2. 全メモリマッピング・全コアロール決定・C-state固定を初期化フェーズで一括実行
3. 「ウォームアップ」フェーズを設け、実運用開始前にキャッシュ・TLB・GPUパイプラインを予熱

---

## 9. セキュリティモデル

汎用OSのような多層防御（ASLR、セキュアブート多段階検証等）はレイテンシに直結するオーバーヘッドを持つものが多いため、取捨選択する：

- **維持するもの**: MMUによるアドレス空間分離（コア間の障害伝播防止のため必須）、起動時のイメージ署名検証（起動時1回のみなので実行時レイテンシに影響しない）
- **放棄するもの**: ASLR（実行時のTLB/キャッシュ効率を優先。専用アプライアンスでマルチテナントを想定しないため許容）、実行時のケーパビリティチェック（起動時に静的決定するため実行時チェック自体が不要）

---

## 10. 使用言語の選定

すべて静的型付け言語のみを使用する（動的型付け言語・GC言語は最悪ケース遅延を保証できないため対象外）。

| コンポーネント | 言語 | 選定理由 |
|---|---|---|
| カーネル本体（Core 0の制御ロジック、リングバッファ、メモリ管理） | **Rust (no_std)** | 借用チェッカーによりuse-after-free/データ競合をコンパイル時に排除できる。ゼロコスト抽象化のためRTオーバーヘッドがない。`unsafe`境界を明示できるため、危険な直叩きコードの範囲をレビューしやすい |
| GPU/NICレジスタ直叩き・ベンダーSDK連携部分（Capture/Encode/Network Domain） | **C++20** | NVENC SDK等ベンダー提供ヘッダが基本的にC/C++ ABI前提であるため、FFI経由よりネイティブ実装の方が安全かつレイテンシ予測しやすい。`constexpr`・`concepts`でコンパイル時検証を強化し、例外・RTTIは無効化した「フリースタンディングC++20」として使う |
| ブートストラップ・低レベルアセンブリ（GDT/IDT設定、AP起動シーケンス等） | **アセンブリ (NASM)** | Rust/C++だけでは書けないCPU初期化の最下層のみに限定使用 |

C言語は単体では採用しない（Rustのunsafeブロック、またはC++のfreestandingサブセットで代替可能であり、型安全性がC++/Rustより弱いため）。ただし将来的にベンダー提供のCヘッダをFFIで取り込む場合はその限りではない。

Rustクレートは `#![no_std]` かつ `alloc` クレート不使用（起動時プール確保のみ）を徹底する。C++側は `-fno-exceptions -fno-rtti -ffreestanding` を必須コンパイルオプションとする。Rust側とC++側の境界は `extern "C"` の薄いFFI層に限定し、双方でメモリ所有権の受け渡しルールをコメントで明記する。

---

## 11. 開発環境・ツールチェーン（Windows 11ネイティブ前提）

**AIコーディングエージェントの実行環境はWindows 11である。** WSL2やLinux専用ツール（Makefile＋bashスクリプト、Linux専用GDBビルド等）を前提にせず、Windows上でネイティブに完結するツールチェーンを選定する。

| 要素 | 選定 | 理由 |
|---|---|---|
| Rustツールチェーン | `rustup`（Windows向け公式インストーラ） + `x86_64-unknown-none` ターゲット | rustup・cargo・rust-lldはWindows上でネイティブ動作し、WSLを必要としない |
| リンカ | `rust-lld`（rustupに同梱） | GNU ldに依存しない。Windows上でもクロスリンクが完結する |
| C++コンパイラ | LLVM/Clang for Windows（`clang++.exe`、`--target=x86_64-unknown-none-elf` 相当のフリースタンディングターゲット指定） | MSVC(cl.exe)はWindows ABI前提でベアメタル出力に不向き。MinGWも同様の制約があるため、クロスターゲットを直接指定できるclangを採用 |
| アセンブラ | NASM for Windows (`nasm.exe`) | Windows向け公式バイナリが提供されており追加のUnix互換層が不要 |
| ビルドオーケストレーション | Cargoの **xtaskパターン**（`cargo xtask build` 等、Rust製の補助バイナリでビルド手順を記述） | Makefile/bashに依存せず、Windows/他OSどちらでも同一のRustコードでビルド手順を記述できる。PowerShellスクリプトへの分散を避け、ロジックを一箇所に集約する |
| 実機検証前のエミュレータ | QEMU for Windows（公式Windowsビルド、`qemu-system-x86_64.exe`） | Windows上でネイティブ動作し、WSL越しのUSB/GPUパススルー問題を回避できる |
| デバッガ | LLDB for Windows（LLVMに同梱）＋ QEMUのgdbstub機能 | Linux専用ビルドが多いGDBより、Windows公式バイナリがあるLLVMのLLDBの方が環境構築が安定する |
| CI/自動検証 | xtask経由でビルド・QEMU起動・シリアル出力アサーションまでを一気通貫でRustコードとして実装 | シェルスクリプトの環境差異（改行コード、パス区切り等）に起因する再現性問題を避ける |

**Windows 11環境で特に注意すべき点:**
- パス区切りは `\` 前提のコードを書かない。Rust/xtask内は `std::path::Path` を使い、OS依存を吸収する
- 改行コードはリポジトリ全体で `.gitattributes` によりLF固定にし、NASM/ClangがCRLFを誤解釈しないようにする
- 管理者権限が必要な操作（QEMUのTAPネットワークデバイス作成等、Windows特有の手順が必要）は、その都度PowerShellでの実行手順を明記する
- Visual Studio Build Toolsのインストールが必要な場合でも、実際のコンパイルはclangで行い、Build Toolsは補助的なCRT/リンカコンポーネントの提供のみに留める

---

## 12. 検証方針

レイテンシ回帰テストをCIの一級市民として扱う。「動くこと」ではなく「レイテンシ予算内に収まること」をテストの合格基準とし、xtask経由で自動化する。

---

## 13. 開発ロードマップ（段階的検証）

1. **Phase 0**: QEMU上でCore 0（マイクロカーネル）のみ起動、シリアル出力確認
2. **Phase 1**: 静的コア割当＋ポーリングループの実装、レイテンシ計測基盤（TSCベースの高精度タイマ）を先に作る
3. **Phase 2**: 実機でGPU capture domainを実装（既存のDXGI/NVENC実装知見を移植）
4. **Phase 3**: ネットワークドメイン（カーネルバイパスNIC + QUIC）の実装
5. **Phase 4**: エンドツーエンドでのglass-to-glass計測、8ms予算達成の検証とチューニング
6. **Phase 5: 静的リアルタイムファイルシステム (LatencyFS) & 時間駆動型 Unix ミニマリストシェル (Pulse Shell)**
   - **実装内容**: インメモリ静的ブロックファイルシステム（$O(1)$ 静的ブロック割当・ゼロ断片化）、時間駆動型プロンプト `[c0|18ns] %`（直前コマンドのハードウェア TSC 実行遅延を自動計測）、時限ガード `within <time> <cmd>`（ハードウェア予算超過検知）、階層パス解決（`cd`, `pwd`, `ls`, `cat`, `mkdir`, `rm`, `cp`, `mv`）、リアルタイムテレメトリコマンド群（`timeline`, `ring`, `cores`, `tsc`, `status`, `pipeline`, `latency`, `benchmark`, `congestion`, `power`, `pci`）。
   - **受け入れ基準**: 階層ディレクトリ操作と相対パス解決が完全動作し、各コマンドの実行遅延がナノ秒単位でプロンプトに表示され、`within` ガードが予算超過時に適切にフォールバックすること（自動テスト `test-boot` にて検証）。
7. **Phase 6: AIネイティブDSL (PulseLang v2) & 独自64-bitレジスタマシンアーキテクチャ (`px64` ISA & VM)**
   - **実装内容**: 20レジスタ構成（16 GPR `$rax`〜`$r15` ＋ 4 HW DMAスロット `#f0`〜`#f3`）、32-bit固定長命令エンコーディング（`PX64_OP_*`）、16バイト固定ヘッダーバイナリ（`PX64`）、単一パスコンパイラ、ゼロヒープVM（`PX64VM`）、コマンドライン引数（`@argc()`, `@arg(idx)`）、引数付きスクリプト実行（`run <file> [args...]`）、逆アセンブラ（`disasm`）。
   - **受け入れ基準**: 全標準スクリプト（`stream.pl`, `bench.pl`, `filter.pl`, `jitter.pl`, `telemetry.pl`, `echo.pl`）がコンパイル・実行可能であり、`disasm` で正確なx64レジスタ名が出力され、引数付きバイナリ実行が正常に動作すること（自動テスト `test-px64` にて検証）。
8. **Phase 7: カーネル内蔵フルスクリーンエディタ (PulseEditor) & 開発者体験・AI連携ツールチェーン**
   - **実装内容**: ANSIフルスクリーンエディタ、Nano風固定ショートカットバー（`[^S / F2 Save]  [^R / F5 Run]  [^Q / F10 Quit]  [^X Save&Quit]  [Esc C Clear]`）、UART一括バッファドレインによる超高速コード貼り付け（Paste、文字欠落・重複改行の完全排除）、DELETEキーの前方文字削除と残像クリーンアップ、機械可読 AI 診断エラー出力（`[ERROR_CODE]`, `[LOCATION]`, `[HEX_DUMP]`, `[AI_REPAIR_HINT]`）。
   - **受け入れ基準**: 長大なスクリプトを一括ペーストしても文字欠落が一切発生せず、DELETEキーで画面の再描画崩れが起きず、文法エラー時にAIが自律修復可能な構造化診断ログが出力されること（自動テスト `test-paste`, `test-compile-error`, `test-editor-delete` にて検証）。
9. **Phase 8: 実効時間ガード (Wall-Clock Watchdog) & AIエラー診断テンプレート分離**
   - **実装内容**: TSCベースの5.0ms壁時計タイムアウトガード（最悪保証時間 5.48ms）、文法エラー（構文・トークン・行コンテキスト）と実行時エラー（タイムアウト・ステップ超過・定数プール範囲外等）の診断テンプレート完全分離。
   - **受け入れ基準**: 敵対的 `@capture()` 無限ループスクリプトが 5.0ms 壁時計ガードにより強制中断され（`ERR_PX64_TIMEOUT_EXCEEDED`）、構文情報のない適切なランタイムエラー診断が出力されること（自動テスト `test-px64` にて検証）。
10. **Phase 9: px64命令フォーマットのリファクタリング (64-bit定数プール & 即値演算拡張)**
   - **実装内容**: 32-bit固定長命令フォーマットを維持したまま、16-bitインデックスによる64-bit定数プールロード命令（`0x17 LDC`）および8-bit即値演算命令（`0x18 ADDI`, `0x19 SUBI`）を追加。`PX64 v3` バイナリフォーマット（定数プール数エントリ追加）、定数プール範囲外境界チェック（`ERR_PX64_CONST_OUT_OF_BOUNDS`）、未登録オペコード検知（`ERR_PX64_INVALID_OPCODE`）、逆アセンブラ（`disasm`）のオペコード/定数プール対応、実測VM命令マイクロベンチマーク計測（QEMU TCG実測: LDC 98ns, ADDI 89ns, Decode 74ns; ベアメタルC0推定: LDC ~5ns, ADDI ~3ns, Decode ~2-3ns）。
   - **受け入れ基準**: 64-bit整数定数（>65535）が `LDC` 1命令でロード可能となり、`ADDI`/`SUBI` によりコードサイズと命令ステップが削減され、全6種の標準バイナリが正常動作すること。また、範囲外定数アクセスや未登録オペコードが適切にトラップされ、逆アセンブラおよびVMマイクロベンチマークで動作検証されていること（自動テスト `test-px64` にて検証）。

各フェーズの完了条件は「機能が動くこと」だけでなく「レイテンシ予算・受け入れ基準を満たし、自動テストで検証されていること」とする。

---

## 14. 既知のトレードオフ（正直な弱点）

- CPU使用率が常時100%になるため、消費電力・発熱は汎用OSより大幅に悪化する
- マルチユーザー・マルチプロセスのような汎用OSの利便性は一切ない
- 対応ハードウェアが極端に狭い
- 「公平なスケジューリング」がないため、複数の重い処理を同時に走らせる用途には根本的に不向き
- **Control Plane（Core 0）の監視遅延**: Core 0 がスクリプト実行（最悪 5.48 ms）やエディタの全バッファペースト（最悪 5.0 ms）を処理している間、同一コア上のウォッチドッグ監視・テレメトリ集計が最悪 5.48 ms 遅延（一時停止）する。ただしストリーミングパイプライン（Core 1〜3）はハードウェア分離された別コアで自律動作するため、パケット伝送の遅延予算（8.0 ms）には影響しない。

これらは「バグ」ではなく「設計上意図的に選択したトレードオフ」であることを明記する。

---

## 15. まとめ

LatencyOSの差別化の核心は、機能の多さではなく **「何を捨てるか」を明確にした引き算の設計** にある。汎用OSが40年かけて積み上げてきた「公平性」「省電力」「互換性」という価値観を、この専用OSでは意図的に持たない。その代わりに、既存のOSでは構造的に到達できないレベルの決定論的低遅延を実現する。
