[package]
name = "pulc-lsp"
version = "0.1.0"
edition = "2021"
description = "Dedicated PulseLang Language Server Protocol (LSP) standalone binary for LatencyOS"
license = "MIT OR Apache-2.0"

[dependencies]
pulselang-core = { path = "../pulselang-core", features = ["std"] }
